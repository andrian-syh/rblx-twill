---
// Renders nothing. The site is dark only (design.md declares "Theme: dark" and
// forbids a light body background), so a theme selector would offer a choice
// that has no effect. src/styles/custom.css defines the light-theme block to
// the same values for anyone whose localStorage still says 'light'.
---
